# ZenPanel (HTML)

Plain HTML / CSS / JavaScript admin UI shell — no React required.

## Scripts

```bash
npm install
npm run dev
```

Open [http://localhost:5173/admin/login](http://localhost:5173/admin/login).

Preview credentials: `admin` / `admin`.

## Customize

Edit `js/config.js` for branding and sidebar navigation.
