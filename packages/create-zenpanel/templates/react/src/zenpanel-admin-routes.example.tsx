/**
 * Example for install-into-existing projects.
 *
 *   import { zenPanelAdminRoute } from "@/routes/admin-routes";
 *
 *   <Routes>
 *     <Route path="/" element={<HomePage />} />
 *     {zenPanelAdminRoute}
 *   </Routes>
 *
 * Also wrap the app with ThemeProvider and import ./admin.css.
 */
export { zenPanelAdminRoute } from "@/routes/admin-routes";
