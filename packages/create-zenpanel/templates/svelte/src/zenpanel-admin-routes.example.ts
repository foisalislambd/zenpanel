/**
 * Example: mount ZenPanel admin routes inside an existing Svelte app.
 * Import admin route IDs from routes/admin-routes.ts and mirror App.svelte layout nesting.
 */
export { adminRouteIds } from "@/routes/admin-routes";
